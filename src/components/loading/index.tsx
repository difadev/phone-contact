import {ContainerLoading, TextTitle} from './styles'

export default function Loading() {
  
    return (
        <ContainerLoading>
            <TextTitle data-testid="textLoadingTest">Loading ...</TextTitle>
        </ContainerLoading>
    )
  }